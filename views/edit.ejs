<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit User</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 flex items-center justify-center min-h-screen">

    <form action="/update/<%= user._id %>" method="POST" class="bg-white p-6 rounded-xl shadow-lg w-80 flex flex-col gap-4">
        <h1 class="text-xl font-bold mb-2">Edit User</h1>

        <input type="text" name="name" value="<%= user.name %>" placeholder="Name" class="border p-2 rounded">
        <input type="text" name="email" value="<%= user.email %>" placeholder="Email" class="border p-2 rounded">
        <input type="text" name="image" value="<%= user.image %>" placeholder="Image URL" class="border p-2 rounded">

        <button type="submit" class="bg-green-600 text-white py-2 rounded hover:bg-green-700">Update</button>
        <a href="/read" class="text-center text-blue-500 hover:underline">Back to Users</a>
    </form>

</body>
</html>
